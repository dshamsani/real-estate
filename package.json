 {
  "name": "pro-house-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run dev",
    "clean": "turbo run clean && rm -rf node_modules",
    "frontend": "pnpm --filter @pro-house/frontend dev",
    "backend": "pnpm --filter @pro-house/backend dev"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
